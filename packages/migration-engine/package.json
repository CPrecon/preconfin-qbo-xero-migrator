{
  "name": "@preconfin/migration-engine",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@preconfin/canonical-model": "0.1.0"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
